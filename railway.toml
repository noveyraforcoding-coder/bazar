[build]
builder = "nixpacks"

[deploy]
startCommand = "gunicorn bazarna.wsgi:application --bind 0.0.0.0:$PORT --workers 2 --timeout 120 --log-file -"
healthcheckPath = "/ar/"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
